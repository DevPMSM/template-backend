info:
  name: POST_User
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/users"
  body:
    type: multipart-form
    data:
      - name: name
        type: text
        value: Lucas Lopes
      - name: email
        type: text
        value: lucas@gmail.com
      - name: password
        type: text
        value: "@ABc12345678"
      - name: role
        type: text
        value: admin
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
