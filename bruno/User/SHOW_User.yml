info:
  name: SHOW_User
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/users/{{lastUserIdCreated}}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
