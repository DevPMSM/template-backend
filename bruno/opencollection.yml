opencollection: 1.0.0

info:
  name: bruno
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  scripts:
    - type: before-request
      code: req.setHeader('Accept', 'application/json');
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
